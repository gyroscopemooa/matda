# CODEX GUI / ASTRA MASTER PROMPT — FINAL

아래 지시는 이 프로젝트의 최상위 규칙이다. 다른 세부 문서와 충돌할 경우 **최신 `DECISIONS_LOG.md`와 이 문서의 핵심 원칙**을 우선한다.

---

## A. 작업 전 반드시 할 일

1. 저장소 전체 구조를 확인한다.
2. `BUILD_PROGRESS.md`와 `DECISIONS_LOG.md`를 먼저 읽는다.
3. `00_README_START_HERE.md`의 문서 순서대로 필요한 명세를 읽는다.
4. `references/`의 실제 이미지를 열어서 본다. 텍스트만 보고 임의의 전형적인 SaaS UI를 만들지 않는다.
5. 현재 코드가 있다면 기존 기술스택, 파일구조, 컴포넌트, DB migration을 파악한다.
6. 사용자가 명시하지 않은 대규모 리팩터링·스택 변경·UI 전면교체는 하지 않는다.

작업 전 반드시 아래 형식으로 현재 계획을 출력한다.

```text
현재 Phase:
현재 구현 완료:
이번 작업 목표:
이번 작업에서 수정할 영역:
절대 건드리지 않을 영역:
필요한 외부 연결/환경변수:
테스트 계획:
완료 후 다음 권장 단계:
```

---

## B. 제품 발전 방식

제품은 다음 순서로 발전한다.

`커뮤니티 → 민간 견적 → 업체 탐색 → MATDA BIZ 업체구함 → B2B 비교견적/RFQ → 계약/사업장 관리 → 민간입찰 → 결제/정산 고도화`

### 매우 중요
- 앞 단계가 활성화되지 않았다는 이유로 다음 핵심 소프트웨어 단계 개발을 포기하지 않는다.
- Phase 1~4의 시장 KPI가 낮아도 **제품의 핵심 거래루프를 완성하기 위해 개발은 계속한다.**
- 사용량이 낮으면 `UI 개선/시드 콘텐츠/업체 모집/SEO/온보딩`을 함께 추천하되 다음 개발 자체를 중단시키지 않는다.
- 다만 돈이 크게 드는 외부 사업, 전문 인력 채용, 직접 대행업 등록, 부지/충전 인프라 등은 소프트웨어와 별개다.

---

## C. 디자인 연속성 규칙

이 프로젝트에서 가장 중요한 UX 원칙이다.

### 1. 단계마다 새 사이트를 만들지 말 것
초기에는 커뮤니티가 홈의 주인공이고, 이후 견적/업체/기업 기능이 **같은 UI 안에서 승격**된다.

`Hidden → Accessible → Promoted → Core`

기능이 추가되면:
- 기존 기능 삭제 X
- 새로운 홈페이지로 교체 X
- 기존 디자인시스템 유지
- 홈 섹션 순서/크기/CTA 우선순위만 조정
- 헤더에 필요한 메뉴만 점진적으로 추가

### 2. 모바일 기본 뼈대
가능한 한 유지:
- 상단: 브랜드 / 지역 / 검색 / 필요 시 알림
- 하단: `홈 / 글쓰기·요청 / 채팅 / MY`

### 3. PC
모바일을 단순 확대하지 않는다.
동일한 카드/색/타이포를 유지하되 넓은 화면에서는:
- 좌: 카테고리/필터
- 중: 핵심 피드/요청
- 우: 내 활동/추천/CTA
형태의 2~3단 배치를 사용할 수 있다.

배너가 많은 포털형 화면을 기본으로 만들지 않는다.

---

## D. 글쓰기/요청 UX

사용자는 길고 복잡한 질문지를 작성하지 않는다.

### 소비자 기본 필수
- 내용(또는 제목+내용)
- 지역
- 대분류 카테고리

### 선택
- 사진
- 희망일
- 예산
- 카테고리별 보조정보

정보가 부족해도 등록을 막지 않는다.

```text
더 정확한 견적을 받으려면 사진과 희망일을 추가하면 좋아요.
[추가하기] [그냥 등록]
```

항상 `그냥 등록` 경로가 존재해야 한다.

---

## E. 초기 커뮤니티
초기 사용자는 그냥 글/댓글/채팅으로 사람을 구할 수 있다.

글 타입 기본:
- 구해요
- 질문
- 후기

옵션/추후:
- 정보
- 업체글/홍보

커뮤니티는 추후 견적 기능이 생겨도 삭제하지 않는다.

---

## F. 민간 견적 기본정책

기본값은 설정값으로 구현한다. 하드코딩하지 않는다.

- 기본 모집기간: 72시간
- 기본 견적 수: 5개
- 5개 도달 시 1차 모집 마감
- 고객이 원하면 추가 5개 오픈 가능
- 최대 견적: 10개
- 연장: 24시간 단위, 기본 최대 2회
- 최대 모집기간 기본값: 5일
- 동일 내용 반복요청은 방지
- 고객 활성요청 기본 상한: 5개
- 동일 카테고리 동시 활성요청 기본 상한: 2개
- 초기 업체 견적 제출은 무료, 지나치게 낮은 월 제한을 두지 않는다.

### 업체 견적 최소필수
- 금액
- 한줄 설명

선택:
- 가능일
- 예상시간
- 포함범위
- 추가비용 조건
- 첨부 견적서

업체는 개인 템플릿을 저장해 재사용할 수 있게 설계한다.

---

## G. 연락/거래

초기 정책:
- 선택 전 전화번호/이메일 직접 노출하지 않는다.
- 내부채팅 무료.
- 고객이 업체를 선택한 뒤 정책에 따라 연락처 공개 가능.
- 업체 수락 단계를 사용할지 여부는 설정 가능하게 한다.

플랫폼 외부 거래가 가능하므로 `선택됨`과 `실제 거래완료`를 구분한다.

고객:
`이 업체와 실제 거래하셨나요?`

업체:
`작업이 완료됐나요?`

양측 응답을 별도 기록한다.

---

## H. MATDA BIZ 핵심 구조

BIZ는 네 가지 층으로 이해한다.

1. **업체/대행업체 구함** — 가장 가벼운 게시형 연결
2. **비교견적/RFQ** — 동일 조건으로 여러 업체의 구조화 견적/제안 비교
3. **민간 공개입찰** — 별도 조달 모듈. 마감/봉인/개찰/평가/선정 절차
4. **계약·사업장 관리** — 반복계약, 만료, 점검, 재견적

### 중요한 개념
- 2번은 1번의 구조화된 고급형이다.
- 3번 입찰은 1·2와 성격이 다르므로 별도 데이터/상태 머신으로 구현한다.
- 공공기관의 법정 나라장터 절차를 대체한다고 표현하지 않는다.
- 민간 기업용 조달/전자입찰 도구로 설계한다.

---

## I. 입찰 MVP 규칙

처음부터 나라장터 전체를 복제하지 않는다.
1차 입찰은 **민간 공개 제안입찰** 하나만 지원한다.

필수:
- 공고 제목/내용
- 발주기업
- 카테고리/지역
- 참가조건
- 시작/마감일
- 선정방식 설명
- 첨부파일
- 입찰가격
- 제안서/서류 첨부
- 마감 전 투찰 수정/철회 정책
- 서버 기준 마감
- 마감 후 개찰
- 평가/선정
- 유찰/취소
- 감사로그

### 파일
PDF/XLSX/DOCX/ZIP/이미지/DWG 등 **첨부파일 업로드/다운로드만** 지원하면 된다.
CAD/도면 편집 프로그램을 직접 만들지 않는다.

### 1차에서는 하지 않는 것
- 자동 최저가 낙찰
- 복잡한 적격심사 엔진
- 나라장터식 모든 경쟁방식
- 자체 CAD/PDF 편집기

---

## J. 결제/정산

MVP에서는 무료이므로 결제를 구현하지 않아도 된다.

유료화 시에도 처음부터 `PG 분리정산 필수`로 고정하지 않는다.
다음 세 층을 분리한다.

1. 결제수단
2. 내부 정산원장
3. 실제 지급

내부 원장은 초기부터 확장 가능하게 설계할 수 있으나, 실제 돈 보관·에스크로·사용자 지갑 기능은 별도 규제 검토 없이 구현하지 않는다.

향후:
`수동/단순 지급 → 대량이체 → 은행 API/PG 지급대행·분리정산 → 자동대사`

전문가/업체 MY에는 `보유캐시`가 아니라 **정산예정금** 개념을 사용한다.
세금/원천징수율을 코드에 3.3%로 고정하지 않는다.

---

## K. 기술 원칙
기존 프로젝트 스택이 있으면 유지한다.
신규라면 권장:
- Next.js + TypeScript
- Supabase Auth/Postgres/Realtime
- Cloudflare DNS/CDN/Turnstile
- Cloudflare R2 이미지/첨부

### 파일보안
- 커뮤니티 이미지: 공개 또는 제한형 정책 가능
- 채팅 첨부: 참여자 전용
- B2B 견적서/계약서/입찰문서: **비공개 bucket + signed URL + 권한검사**

RLS를 반드시 적용한다.

---

## L. 테스트/진행상태

코드 작성만으로 완료 처리하지 않는다.
각 기능은:
1. 정상
2. 빈 상태
3. 로딩
4. 오류
5. 비로그인
6. 권한 오류
7. 모바일
8. PC
9. 새로고침
10. 회귀
테스트 후에만 완료 처리한다.

작업 종료 전 반드시 `BUILD_PROGRESS.md`와 `DECISIONS_LOG.md`를 갱신한다.

출력 형식:

```text
완료한 것:
테스트 결과:
DB/마이그레이션:
외부서비스 연결상태:
남은 문제:
이번 단계 통과 여부:
다음 권장 단계:
BUILD_PROGRESS 갱신 내용:
```

---

## M. 에이전트 행동규칙
- 모르는 요구사항을 임의로 크게 확장하지 않는다.
- 그러나 이미 이 문서에서 정해진 세부사항은 사용자가 매번 재설명하지 않아도 적용한다.
- 사용자가 중간에 UI/정책을 바꾸면 `DECISIONS_LOG.md`에 변경사항을 기록한다.
- 기존 기능을 깨뜨리는 변경이 필요하면 먼저 이유와 영향범위를 말한다.
- 모든 단계 후 다음 권장 작업을 제안한다.
- 커뮤니티 지표가 낮다고 프로젝트 포기를 권하지 않는다. 기술 로드맵을 계속 진행하면서 개선안을 병행한다.
- 실제 외부 API 키/결제사 계약/법적 신고가 없어서 활성화할 수 없는 기능은 mock 또는 feature flag로 구현하고 정확한 활성화 체크리스트를 남긴다.


## 2026-09-20 최신 제안 흐름 결정
기존 금액·설명 일률 필수 대신 확정/예상/확인 후 유형별 최소 입력을 사용한다. 공통 질문은 요청자 동의 후 제안 참여 업체에게 한 번에 공유하고, 개별 상담은 채팅으로 진행한다. 확정 견적만 선택 가능하다. 상세 최신 기준: docs/PROPOSAL_WORKFLOW.md
